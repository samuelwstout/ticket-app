import Navigation from "../Navigation"

const MyConcerts = ({setUser, user}) => {
  
  return (
    <div>
      <Navigation setUser={setUser} user={user} />
    </div>
  )
}

export default MyConcerts